#include <stdio.h>
int main(){

    int marks[90]; //Reserve space to store 90 integer
      marks[0]=45;
      marks[1]=50;
      //we can go all the way till marks[89]
      printf("The value of marks 0 and 1 is %d and %d",marks[0],marks[1]);
      return 0;
}